<?php
/*
Plugin Name: My Plugin
Plugin URI:
Description:
Author:
Version:
Author URI:
*/

/** Absolute path to plugin directory (with trailing slash). */
define( 'MY_PLUGIN_DIR', trailingslashit( __DIR__ ) );

/** Public URL to plugin directory (with trailing slash). */
define( 'MY_PLUGIN_URL', trailingslashit( plugin_dir_url( __FILE__ ) ) );